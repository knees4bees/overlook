class BookingsRepo {
  constructor(bookingsData, customers, rooms) {
    this.bookings = this.createBookings();
  }

  createBookings() {
    // TODO figure this out
  }
};

export default BookingsRepo;